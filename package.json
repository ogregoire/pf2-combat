{
  "name": "pf2-combat",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "data": "tsx packages/pf2data/src/cli.ts",
    "app": "npm run dev -w @pf2/app",
    "build:app": "npm run build -w @pf2/app",
    "preapp": "node scripts/link-data.mjs",
    "prebuild:app": "node scripts/link-data.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
